# Core CV / numerics
numpy>=1.26
opencv-python-headless>=4.9
# Async S3 streaming (Stage 0)
aioboto3>=12.0
boto3>=1.34
# Fast JPEG decode (optional but recommended)
PyTurboJPEG>=1.7
# Deep learning backends (Stage 1/3/5/6)
torch>=2.3
torchvision>=0.18
ultralytics>=8.2          # RT-DETR detector
timm>=1.0          # ConvNeXt-V2 backbones for the trained heads
# SAM 2.1 (install from Meta's repo): pip install "git+https://github.com/facebookresearch/sam2.git"
# Horizontal scale-out (optional)
ray>=2.10
# Tests
pytest>=8.0
